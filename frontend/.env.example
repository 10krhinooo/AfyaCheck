# Copy to .env.local (gitignored) and fill in a real value for local dev.
# Baked into the build at build time; the Maps JavaScript API key is not a
# secret at runtime (it ends up client-visible either way) but should still
# be restricted by HTTP referrer in the Google Cloud Console.
VITE_GOOGLE_MAPS_API_KEY=
