# Dev-only overrides. Activate with SPRING_PROFILES_ACTIVE=dev (or
# spring.profiles.active=dev). Keeps the base application.properties clean
# of verbose logging that shouldn't run in production.

spring.jpa.show-sql=true
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
